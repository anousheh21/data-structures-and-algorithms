// My solution to 70 - Climbing Stairs

function climbStairs(n: number): number {
    
};

// Time Complexity:

const testCases = [2, 3];

for (let i = 0; i < testCases.length; i++) {
    const output = climbStairs(testCases[i]);
    console.log(`Output: ${output} for input: ${testCases[i]}`);
}

export {};
